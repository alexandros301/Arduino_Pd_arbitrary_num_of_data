Arduino_Pd_arbitrary_num_of_data
================================

Arduino sketch and Pd patch that can transfer an arbitrary number of data without needing to define it before hand.

Written by Alexandros Drymonitis
